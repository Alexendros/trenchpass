# Pinned toolchain. Si tu entorno no tiene rustup, sigue las instrucciones
# en CONTRIBUTING.md para preparar la toolbox Fedora.

[toolchain]
channel    = "1.88"
profile    = "minimal"
components = ["rustfmt", "clippy", "rust-src"]
targets    = []
